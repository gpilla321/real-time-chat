schema:
  - http://localhost:5000/graphql
documents: "graphql/**/*.graphql"
generates:
  ./graphql/schema.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
